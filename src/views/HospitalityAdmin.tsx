'use client';

import { useEffect, useState } from 'react';
import { ExternalLink, Github, ImageIcon, Loader2, LockKeyhole, RefreshCw, Save, Sparkles, Trash2, X } from 'lucide-react';
import type { GeneratedHospitalityDemo } from '@/src/tourism-theme/src/generated/types';

const adminText = (value:unknown) => {
  if(typeof value==='string'||typeof value==='number')return String(value);
  if(value&&typeof value==='object'){
    const localized=value as Record<string,unknown>;
    const selected=localized.ro??localized.en??localized.de??localized.no;
    if(typeof selected==='string'||typeof selected==='number')return String(selected);
  }
  return '';
};

export default function HospitalityAdmin() {
  const [secret,setSecret] = useState(()=>typeof window === 'undefined' ? '' : sessionStorage.getItem('nodestack-admin-secret') || '');
  const [authenticated,setAuthenticated] = useState(false);
  const [sourceUrl,setSourceUrl] = useState('');
  const [demo,setDemo] = useState<GeneratedHospitalityDemo|null>(null);
  const [publishedDemos,setPublishedDemos] = useState<GeneratedHospitalityDemo[]>([]);
  const [json,setJson] = useState('');
  const [busy,setBusy] = useState<'import'|'publish'|'list'|''>('');
  const [message,setMessage] = useState('');
  const [deletingSlug,setDeletingSlug] = useState('');

  useEffect(()=>{ if(demo) setJson(JSON.stringify(demo,null,2)); },[demo]);
  const headers = {'Content-Type':'application/json','x-admin-secret':secret};
  const loadPublished = async (showMessage=true) => {
    if(!secret)return; setBusy('list'); if(showMessage)setMessage(''); sessionStorage.setItem('nodestack-admin-secret',secret);
    try {
      const response=await fetch('/api/list-demos',{headers});
      const value=await response.json(); if(!response.ok){if(response.status===401)setAuthenticated(false);throw new Error(value.error||'Could not load published demos');}
      setAuthenticated(true); setPublishedDemos(value.demos||[]); if(showMessage)setMessage(`Loaded ${value.demos?.length||0} published demos.`);
    } catch(error) { if(showMessage||!authenticated)setMessage(error instanceof Error?error.message:'Could not load published demos'); } finally { setBusy(''); }
  };
  const importListing = async () => {
    setBusy('import'); setMessage(''); sessionStorage.setItem('nodestack-admin-secret',secret);
    try {
      const response=await fetch('/api/import-demo',{method:'POST',headers,body:JSON.stringify({sourceUrl})});
      const value=await response.json(); if(!response.ok) throw new Error(value.error||'Import failed');
      setDemo(value.demo); setMessage('Import complete. Review every generated field before publishing.');
    } catch(error) { setMessage(error instanceof Error?error.message:'Import failed'); } finally { setBusy(''); }
  };
  const applyJson = () => { try { setDemo(JSON.parse(json)); setMessage('JSON changes applied.'); } catch { setMessage('The JSON is invalid.'); } };
  const preview = () => { if(!demo)return; sessionStorage.setItem('nodestack-demo-preview',JSON.stringify(demo)); window.open('/demo/preview','_blank'); };
  const publish = async () => {
    if(!demo)return; setBusy('publish'); setMessage('');
    try {
      const response=await fetch('/api/publish-demo',{method:'POST',headers,body:JSON.stringify({demo})});
      const value=await response.json(); if(!response.ok) throw new Error(value.error||'Publish failed');
      setMessage(`Committed to GitHub. The demo will be available after deployment: ${value.path}`);
      await loadPublished(false);
    } catch(error) { setMessage(error instanceof Error?error.message:'Publish failed'); } finally { setBusy(''); }
  };

  const deleteDemo = async (item:GeneratedHospitalityDemo) => {
    if(!window.confirm(`Delete ${adminText(item.property.name)}? This commits the deletion to GitHub and removes the live demo after deployment.`))return;
    setDeletingSlug(item.slug); setMessage('');
    try {
      const response=await fetch('/api/delete-demo',{method:'DELETE',headers,body:JSON.stringify({slug:item.slug})});
      const value=await response.json(); if(!response.ok) throw new Error(value.error||'Delete failed');
      setPublishedDemos(current=>current.filter(entry=>entry.slug!==item.slug));
      if(demo?.slug===item.slug){setDemo(null);setJson('');}
      setMessage(`Deleted ${adminText(item.property.name)} from GitHub. The live route will disappear after deployment.`);
    } catch(error) { setMessage(error instanceof Error?error.message:'Delete failed'); } finally { setDeletingSlug(''); }
  };

  const toggleRoomImage = (roomIndex:number,image:string) => setDemo(current=>{
    if(!current)return current;
    const rooms=current.rooms.map((room,index)=>index!==roomIndex?room:{...room,images:room.images.includes(image)?room.images.filter(item=>item!==image):[...room.images,image]});
    return {...current,rooms};
  });
  const clearRoomImages = (roomIndex:number) => setDemo(current=>current?{...current,rooms:current.rooms.map((room,index)=>index===roomIndex?{...room,images:[]}:room)}:current);
  const setHeroImage = (image:string) => setDemo(current=>current?{...current,heroImage:image}:current);
  const removePropertyImage = (image:string) => {
    if(!window.confirm('Remove this photo from the demo, all rooms and attractions?'))return;
    setDemo(current=>{
      if(!current)return current;
      const images=current.images.filter(item=>item!==image);
      return {...current,images,heroImage:current.heroImage===image?(images[0]||''):current.heroImage,rooms:current.rooms.map(room=>({...room,images:room.images.filter(item=>item!==image)})),attractions:current.attractions.map(attraction=>attraction.image===image?{...attraction,image:images[0]||''}:attraction)};
    });
  };
  const addAttraction = () => setDemo(current=>current?{...current,attractions:[...current.attractions,{title:'',description:'',distance:'\u00cen apropiere',image:current.images[0]||'',source:'manual'}]}:current);
  const updateAttraction = (index:number,field:'title'|'description'|'distance'|'image',value:string) => setDemo(current=>current?{...current,attractions:current.attractions.map((item,itemIndex)=>itemIndex===index?{...item,[field]:value,source:'manual'}:item)}:current);
  const removeAttraction = (index:number) => setDemo(current=>current?{...current,attractions:current.attractions.filter((_,itemIndex)=>itemIndex!==index)}:current);
  const updateGoogleListing = (field:'googleMapsUrl'|'googleRating'|'googleReviewCount',value:string) => setDemo(current=>{
    if(!current)return current;
    return {...current,property:{...current.property,[field]:field==='googleMapsUrl'?value:Number(value)}};
  });

  if(!authenticated) return <main className="min-h-screen bg-slate-950 text-slate-200 grid place-items-center p-4"><section className="w-full max-w-md rounded-3xl border border-white/10 bg-white/[.035] p-7 sm:p-9"><div className="h-12 w-12 rounded-2xl bg-emerald-500/10 text-emerald-400 grid place-items-center mb-6"><LockKeyhole/></div><p className="text-emerald-400 font-mono text-xs uppercase tracking-widest">Protected internal tool</p><h1 className="text-3xl font-bold mt-3">Admin access</h1><p className="text-slate-500 text-sm mt-3">Verify the admin secret to access generated properties and publishing controls.</p><label className="block text-xs font-bold text-slate-400 mt-7">Admin secret<input autoFocus type="password" value={secret} onChange={e=>setSecret(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')loadPublished(false);}} className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label>{message&&<p className="mt-4 text-sm text-red-300">{message}</p>}<button disabled={!secret||!!busy} onClick={()=>loadPublished(false)} className="mt-5 h-12 w-full rounded-xl bg-emerald-600 hover:bg-emerald-500 disabled:opacity-40 px-6 font-bold text-white flex items-center justify-center gap-2">{busy==='list'?<Loader2 className="animate-spin"/>:<LockKeyhole className="h-5 w-5"/>}Unlock dashboard</button><a href="/" className="block text-center text-sm text-slate-500 mt-6">← NodeStack</a></section></main>;

  return <main className="min-h-screen overflow-x-hidden bg-slate-950 text-slate-200 p-4 sm:p-8"><div className="max-w-7xl mx-auto">
    <div className="flex flex-col sm:flex-row justify-between gap-5 mb-10"><div><p className="text-emerald-400 font-mono text-xs uppercase tracking-widest">Internal tool · noindex</p><h1 className="text-3xl sm:text-5xl font-bold mt-3">Hospitality demo generator</h1><p className="text-slate-500 mt-3">Import a TuristInfo listing, review AI-assisted demo data, then publish through GitHub.</p></div><a href="/" className="text-sm text-slate-500">← NodeStack</a></div>
    <section className="rounded-3xl border border-white/10 bg-white/[.035] p-6 mb-7"><div className="grid lg:grid-cols-[.7fr_1.6fr_auto] gap-4 items-end"><label className="text-xs font-bold text-slate-400">Admin secret<input type="password" value={secret} onChange={e=>setSecret(e.target.value)} className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label><label className="text-xs font-bold text-slate-400">TuristInfo listing URL<input type="url" value={sourceUrl} onChange={e=>setSourceUrl(e.target.value)} placeholder="https://www.turistinfo.ro/..." className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label><button disabled={!secret||!sourceUrl||!!busy} onClick={importListing} className="h-12 rounded-xl bg-emerald-600 hover:bg-emerald-500 disabled:opacity-40 px-6 font-bold text-white flex items-center justify-center gap-2">{busy==='import'?<Loader2 className="animate-spin"/>:<Sparkles/>}Import & generate</button></div></section>
    {message&&<div className="rounded-xl border border-blue-400/20 bg-blue-400/10 px-5 py-4 mb-7 text-sm text-blue-100">{message}</div>}
    {demo&&<section className="rounded-3xl border border-white/10 bg-white/[.035] p-6 mb-7"><div className="flex flex-col lg:flex-row lg:items-end gap-4"><div className="lg:w-64 shrink-0"><p className="text-xs uppercase tracking-widest text-slate-500">Google listing</p><h2 className="text-xl font-bold mt-1">Rating shown in demo</h2><p className="text-xs text-slate-500 mt-2">Enter the current listing values before previewing or publishing.</p></div><label className="min-w-0 flex-1 text-xs font-bold text-slate-400">Google Maps or Search URL<input type="url" value={demo.property.googleMapsUrl||''} onChange={event=>updateGoogleListing('googleMapsUrl',event.target.value)} placeholder="https://www.google.com/maps/place/..." className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label><label className="w-full lg:w-28 text-xs font-bold text-slate-400">Rating<input type="number" min="0" max="5" step="0.1" value={demo.property.googleRating||''} onChange={event=>updateGoogleListing('googleRating',event.target.value)} placeholder="4.9" className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label><label className="w-full lg:w-36 text-xs font-bold text-slate-400">Review count<input type="number" min="0" step="1" value={demo.property.googleReviewCount||''} onChange={event=>updateGoogleListing('googleReviewCount',event.target.value)} placeholder="20" className="mt-2 w-full rounded-xl bg-slate-900 border border-white/10 p-3 text-white outline-none focus:border-emerald-400"/></label></div></section>}
    <section className="rounded-3xl border border-white/10 bg-white/[.035] p-6 mb-7"><div className="flex flex-wrap items-center justify-between gap-4 mb-5"><div><p className="text-xs uppercase tracking-widest text-slate-500">Published demos</p><h2 className="text-xl font-bold mt-1">Created properties</h2></div><button disabled={!secret||!!busy} onClick={()=>loadPublished()} className="rounded-xl border border-white/10 bg-white/5 hover:bg-white/10 disabled:opacity-40 px-4 py-2 text-sm font-bold flex items-center gap-2">{busy==='list'?<Loader2 className="h-4 w-4 animate-spin"/>:<RefreshCw className="h-4 w-4"/>}Load demos</button></div>{publishedDemos.length?<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{publishedDemos.map(item=><article key={item.slug} className="overflow-hidden rounded-2xl border border-white/10 bg-slate-900"><img src={item.images?.[0]} alt="" referrerPolicy="no-referrer" className="h-36 w-full object-cover bg-slate-800"/><div className="p-4"><h3 className="font-bold text-white truncate">{adminText(item.property?.name)||adminText(item.slug)}</h3><p className="text-xs text-slate-500 mt-1">/{adminText(item.slug)}</p><p className="text-xs text-slate-500 mt-2">{item.generatedAt?new Date(item.generatedAt).toLocaleString():''}</p><div className="flex flex-wrap gap-2 mt-4"><a href={`/demo/${item.slug}`} target="_blank" rel="noreferrer" className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white flex items-center gap-1"><ExternalLink className="h-3.5 w-3.5"/>Open</a><button onClick={()=>{setDemo(item);setMessage(`Loaded ${adminText(item.property.name)} into the editor.`);}} className="rounded-lg border border-white/10 px-3 py-2 text-xs font-bold">Edit JSON</button><button disabled={deletingSlug===item.slug} onClick={()=>deleteDemo(item)} className="rounded-lg border border-red-400/20 bg-red-400/10 text-red-300 px-3 py-2 text-xs font-bold flex items-center gap-1 disabled:opacity-40">{deletingSlug===item.slug?<Loader2 className="h-3.5 w-3.5 animate-spin"/>:<Trash2 className="h-3.5 w-3.5"/>}Delete</button></div></div></article>)}</div>:<p className="text-sm text-slate-500">No published demos found.</p>}</section>
    {demo&&<div className="grid xl:grid-cols-[minmax(0,.75fr)_minmax(0,1.25fr)] gap-7"><aside className="min-w-0 space-y-5"><div className="rounded-3xl border border-white/10 bg-white/[.035] p-6"><p className="text-xs uppercase tracking-widest text-slate-500">Imported property</p><h2 className="text-2xl mt-2">{adminText(demo.property.name)}</h2><p className="text-sm text-slate-500 mt-2">/{adminText(demo.slug)}</p><div className="grid grid-cols-2 gap-3 mt-6 text-sm"><div className="rounded-xl bg-slate-900 p-3"><b className="block text-white">{demo.images.length}</b>images</div><div className="rounded-xl bg-slate-900 p-3"><b className="block text-white">{demo.reviews.length}</b>real reviews</div><div className="rounded-xl bg-slate-900 p-3"><b className="block text-white">{demo.rooms.length}</b>room cards</div><div className="rounded-xl bg-slate-900 p-3"><b className="block text-white">{demo.facilities.length}</b>facilities</div></div></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-6"><p className="text-xs uppercase tracking-widest text-slate-500">Property gallery</p><p className="text-xs text-slate-500 mt-2 mb-4">Choose the hero or remove unwanted photos before publishing.</p><div className="grid grid-cols-3 gap-2">{demo.images.map((image,i)=><div key={image} className={`relative aspect-square overflow-hidden rounded-lg bg-slate-900 border-2 ${demo.heroImage===image?'border-amber-400':'border-transparent'}`}><img src={image} alt={`Property photo ${i+1}`} referrerPolicy="no-referrer" className="h-full w-full object-cover"/>{demo.heroImage===image&&<span className="absolute left-1 top-1 rounded bg-amber-400 px-1.5 py-0.5 text-[9px] font-black text-slate-950">HERO</span>}<div className="absolute inset-x-1 bottom-1 flex gap-1"><button title="Set as hero" onClick={()=>setHeroImage(image)} className="h-7 flex-1 rounded bg-black/75 text-white grid place-items-center hover:bg-amber-500"><ImageIcon className="h-3.5 w-3.5"/></button><button title="Remove photo" onClick={()=>removePropertyImage(image)} className="h-7 w-7 rounded bg-red-600/90 text-white grid place-items-center hover:bg-red-500"><X className="h-3.5 w-3.5"/></button></div></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-6"><p className="text-xs uppercase tracking-widest text-slate-500">Room images</p><p className="text-xs text-slate-500 mt-2 mb-5">Choose which gallery photos belong to each room. The first selected image is the room cover.</p><div className="space-y-6">{demo.rooms.map((room,roomIndex)=><div key={room.id||roomIndex}><div className="flex items-center justify-between gap-3 mb-3"><h3 className="text-sm font-bold text-white">{adminText(room.title)||`Room ${roomIndex+1}`} <span className="text-slate-500 font-normal">· {room.images.length} selected</span></h3><button onClick={()=>clearRoomImages(roomIndex)} className="text-xs text-red-300 hover:text-red-200">Clear</button></div><div className="grid grid-cols-4 gap-2">{demo.images.map((image,imageIndex)=>{const selected=room.images.includes(image);return <button type="button" key={image} onClick={()=>toggleRoomImage(roomIndex,image)} className={`relative aspect-square overflow-hidden rounded-lg border-2 ${selected?'border-emerald-400 ring-2 ring-emerald-400/20':'border-transparent opacity-60 hover:opacity-100'}`}><img src={image} alt={`Photo ${imageIndex+1}`} referrerPolicy="no-referrer" className="h-full w-full object-cover"/>{selected&&<span className="absolute right-1 top-1 h-5 min-w-5 px-1 rounded-full bg-emerald-500 text-[10px] font-bold text-white grid place-items-center">{room.images.indexOf(image)+1}</span>}</button>})}</div></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-widest text-slate-500">{'Atrac\u021bii \u00een apropiere'}</p><p className="text-xs text-slate-500 mt-2">Complete the places shown next to the map.</p></div><button onClick={addAttraction} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white">Add</button></div><div className="space-y-5 mt-5">{demo.attractions.map((attraction,index)=><div key={index} className="rounded-2xl border border-white/10 bg-slate-900 p-4"><div className="flex justify-between gap-3 mb-3"><span className="text-xs font-bold text-white">Attraction {index+1}</span><button onClick={()=>removeAttraction(index)} className="text-xs text-red-300">Remove</button></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-2"><input value={adminText(attraction.title)} onChange={event=>updateAttraction(index,'title',event.target.value)} placeholder="Name" className="rounded-lg border border-white/10 bg-slate-950 p-2 text-xs text-white outline-none focus:border-emerald-400"/><input value={adminText(attraction.distance)} onChange={event=>updateAttraction(index,'distance',event.target.value)} placeholder="Distance" className="rounded-lg border border-white/10 bg-slate-950 p-2 text-xs text-white outline-none focus:border-emerald-400"/></div><textarea value={adminText(attraction.description)} onChange={event=>updateAttraction(index,'description',event.target.value)} placeholder="Description" className="mt-2 min-h-20 w-full rounded-lg border border-white/10 bg-slate-950 p-2 text-xs text-white outline-none focus:border-emerald-400"/><p className="text-[10px] uppercase tracking-wider text-slate-500 mt-3 mb-2">Photo</p><div className="flex gap-2 overflow-x-auto pb-2">{demo.images.map(image=><button type="button" key={image} onClick={()=>updateAttraction(index,'image',image)} className={`h-14 w-16 shrink-0 overflow-hidden rounded-lg border-2 ${attraction.image===image?'border-emerald-400':'border-transparent opacity-60'}`}><img src={image} alt="" referrerPolicy="no-referrer" className="h-full w-full object-cover"/></button>)}</div></div>)}</div></div></aside><section className="min-w-0"><div className="flex flex-wrap gap-3 mb-4"><button onClick={applyJson} className="rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm font-bold flex gap-2"><Save className="h-4 w-4"/>Apply JSON</button><button onClick={preview} className="rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm font-bold flex gap-2"><ExternalLink className="h-4 w-4"/>Preview</button><button disabled={!!busy} onClick={publish} className="rounded-xl bg-brand px-4 py-2 text-sm font-bold text-white flex gap-2 disabled:opacity-40">{busy==='publish'?<Loader2 className="h-4 w-4 animate-spin"/>:<Github className="h-4 w-4"/>}Commit & deploy</button></div><textarea value={json} onChange={e=>setJson(e.target.value)} spellCheck={false} className="block w-full max-w-full min-h-[75vh] resize-y rounded-2xl bg-[#080d19] border border-white/10 p-5 font-mono text-xs leading-5 text-slate-300 outline-none focus:border-brand"/></section></div>}
  </div></main>;
}
